// Background console'a yapıştır:

chrome.storage.local.get(['trackers'], (result) => {
  const tracker = result.trackers.find(t => t.url.includes('bosgame'));
  
  if (tracker) {
    console.log('📦 Bosgame Tracker:');
    console.log('  URL:', tracker.url);
    console.log('  Selector:', tracker.selector);
    console.log('  exactPriceValue:', tracker.exactPriceValue);
    console.log('  Son fiyat:', tracker.priceHistory[tracker.priceHistory.length - 1]);
  } else {
    console.log('❌ Bosgame tracker yok!');
  }
});
