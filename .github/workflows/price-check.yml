name: Hourly Price Check

on:
  schedule:
    # Her saat başı çalışır (0. dakika)
    - cron: '0 * * * *'
  
  # Manuel tetikleme için (Actions sekmesinden test edebilirsin)
  workflow_dispatch:

jobs:
  check-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🔍 Fiyat Kontrolü Başlat
        run: |
          echo "🚀 Vercel backend'e istek gönderiliyor..."
          
          # Vercel API'yi tetikle
          response=$(curl -s -w "\n%{http_code}" "https://chrome-fiyat-v1.vercel.app/api/cron/check-prices")
          
          # HTTP response code'u ayır
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n-1)
          
          echo "📊 HTTP Status: $http_code"
          echo "📦 Response: $body"
          
          # 200-299 arası başarılı
          if [ "$http_code" -ge 200 ] && [ "$http_code" -lt 300 ]; then
            echo "✅ Fiyat kontrolü başarılı!"
          else
            echo "❌ Hata oluştu!"
            exit 1
          fi
      
      - name: 📊 İstatistikler
        run: |
          echo "⏰ Çalışma Zamanı: $(date)"
          echo "🌍 Timezone: UTC"
          echo "📍 Türkiye Saati: $(TZ=Europe/Istanbul date)"
